AuditMate Invariants

Core Rules

1. Collectors must never print
2. Collectors must never exit
3. audit.Run() must be deterministic
4. Diff must be pure logic
5. Free and Pro must share scan engine
6. Signing must never alter baseline data
7. Exit codes must remain stable
8. CLI must remain script-friendly
9. Output must remain human-readable
10. Tool must always work offline